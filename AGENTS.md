## Git operations

Ask the user before running any git command that writes to the repository: commit, push, pull, rebase, merge, checkout, reset, clean, stash, amend, revert, cherry-pick. Read-only commands (status, log, diff, show, branch) need no approval.

## Agent skills

### Issue tracker

Creating, editing, reading, or wayfinding issues and specs (`.scratch/<feature-slug>/`) — see `docs/agents/issue-tracker.md`.

### Triage labels

When a skill names a triage role and you need our label string — see `docs/agents/triage-labels.md`.

### Domain docs

Before exploring a codebase area, read the glossary (`CONTEXT.md` at the repo root) and ADRs (`docs/adr/`); use the glossary's vocabulary and flag ADR conflicts — see `docs/agents/domain.md`.
